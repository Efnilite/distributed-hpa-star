# Distributed HPA*

The repo for distributed HPA*.

## Structure

- `common/` contains utilities used by all implementations.
- `data/` contains the used test maps
- `a*/` contains the desktop A* implementation used for comparison.
- `hpa*/` contains the desktop HPA* implementation.